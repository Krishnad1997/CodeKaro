const { add, subtract } = require('./mathFunctions');

const sum = add(5, 3);
console.log('Sum of 5 and 3:', sum);

const difference = subtract(10, 4);
console.log('Difference of 10 and 4:', difference);
